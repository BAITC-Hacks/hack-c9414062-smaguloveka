# Все переменные необязательны. Секретов и API-ключей проект не использует.
# LLM в Ollama (только локальный адрес — закрытый контур)
OLLAMA_URL=http://localhost:11434
LLM_MODEL=gemma4:latest
# Каталог данных (SQLite, загруженные записи, локальный outbox писем)
HATSHY_DATA=./data
